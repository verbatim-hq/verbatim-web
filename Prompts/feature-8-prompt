### SYSTEM ROLE & DIRECTIVE
You are an elite, world-class full-stack engineer. You write hyper-optimized, strictly typed Next.js 15 App Router code. You do not write placeholders. You output complete, production-ready code. Apply a "Zero-Any" policy.

### CONTEXT: VERBATIM.AI — FEATURE 8: STRIPE BILLING & PROGRESSIVE PAYWALL
We are implementing the revenue engine: a flat $29/mo subscription. The wedge is a freemium model: 2 free syntheses, followed by a hard paywall. We do not use a dedicated pricing page; instead, we gracefully interrupt the PM's workflow using a modal when they hit their limit.

### CURSOR DIRECTIVES (CRITICAL)
- **Do NOT** touch or rewrite `app/api/inngest/functions.ts`. Our background orchestration pipeline is locked.
- Explicitly read and modify the files tagged below. Do not refactor unrelated code.

### IMPLEMENTATION REQUIREMENTS

#### 1. SQL Migration (`@supabase/migrations/0008_stripe_billing.sql`)
Create a new migration to update the `orgs` table (created in Feature 1):
- `tier` (text, check in ('free', 'pro'), default 'free').
- `stripe_customer_id` (text, unique).
- `stripe_subscription_id` (text, unique).
- **RLS**: The webhook will use `service_role` to update this. Do not alter the existing `orgs` RLS policies.

#### 2. The Billing Utility & Usage Gate (`@lib/billing.ts`)
Create a server-side helper `checkUsageLimit(orgId: string)`:
- Query the `orgs` table to check `tier`. If `'pro'`, return `{ allowed: true }`.
- If `'free'`, query the `sources` table to count rows matching the `org_id`.
- If count `< 2`, return `{ allowed: true }`. If `>= 2`, return `{ allowed: false }`.

#### 3. The Interception (`@app/projects/actions.ts` & Upload Actions)
- Surgically inject `await checkUsageLimit(orgId)` into your existing `startUploadAction` and `synthesizeProjectAction`.
- If `!allowed`, `throw new Error("PAYWALL_LIMIT_REACHED")`.

#### 4. The Stripe Engine (`@app/billing/actions.ts`)
Implement two Server Actions using Zod to validate inputs. Ensure Clerk auth & `current_org_id()`:
- `createCheckoutSessionAction`: Creates a Stripe Checkout Session for the $29/mo Price ID (passed via `process.env.STRIPE_PRICE_ID`). **CRITICAL**: Inject `client_reference_id: orgId` so the webhook can map the payment. Return the session URL.
- `createPortalSessionAction`: Creates a Stripe Customer Portal session using the org's `stripe_customer_id`. Return the portal URL.

#### 5. The Secure Webhook (`@app/api/webhooks/stripe/route.ts`)
- **Next.js 15 Specifics**: Extract the raw body using `const body = await request.text();` to ensure signature verification does not fail.
- Use `stripe.webhooks.constructEvent` with the raw text and `process.env.STRIPE_WEBHOOK_SECRET` to verify the signature.
- **`checkout.session.completed`**: Extract `client_reference_id` (this is our `org_id`), `customer`, and `subscription`. Use a Supabase `service_role` client to update the `orgs` table where `id = client_reference_id`, setting `tier = 'pro'`, `stripe_customer_id`, and `stripe_subscription_id`.
- **`customer.subscription.deleted`**: Look up the org by `stripe_subscription_id` and downgrade `tier = 'free'`, clearing the subscription ID.

#### 6. UI/UX: The Interruption (`@components/billing/paywall-modal.tsx` & `@components/layout/header.tsx`)
- **Paywall Modal**: A `shadcn/ui` Dialog. Ensure it catches the `"PAYWALL_LIMIT_REACHED"` error thrown by the server actions (via a try/catch or action state). 
- **Typography**: Header is 'Instrument Serif' ("Unlock unlimited research synthesis."). Body is 'Geist'.
- **Action**: A primary button (`--color-blue`) saying "Upgrade to Pro - $29/mo". Clicking it triggers `createCheckoutSessionAction` and redirects the user. Add a Framer Motion loading state to the button while resolving.
- **Header Update**: Add a minimalist "Billing" dropdown item in the authenticated header that triggers `createPortalSessionAction` for 'pro' users.

### EXPECTED OUTPUT
1.  Install dependencies: `npm install stripe`.
2.  The `0008_stripe_billing.sql` migration.
3.  The strict `lib/billing.ts` utility.
4.  Updates to the targeted Server Actions to include the usage gate.
5.  The Stripe Server Actions and secure Webhook route.
6.  The `paywall-modal.tsx` component and header integration.


The output:
1) Dependency
You already have stripe in package.json, but the required command is:

cd d:\Verbatim\verbatim
npm install stripe
2) Migration — 0008_stripe_billing.sql
Created verbatim/supabase/migrations/0008_stripe_billing.sql:

Adds orgs.tier (free|pro, default free)
Adds partial unique indexes for stripe_customer_id + stripe_subscription_id
No RLS policy changes (webhook updates via service_role)
3) Billing gate — lib/billing.ts
Added checkUsageLimit(orgId: string) (server-only):

If tier === "pro" → allowed
If free → counts sources rows for org_id; <2 allowed, >=2 blocked
File: verbatim/lib/billing.ts

4) Interception (surgical) — app/projects/actions.ts
Injected:

await checkUsageLimit(orgId) into startUploadAction and synthesizeProjectAction
If blocked → throw new Error("PAYWALL_LIMIT_REACHED")
No other refactors.

5) Stripe engine
Server actions — app/billing/actions.ts
Created:

createCheckoutSessionAction({ returnPath? })
Uses STRIPE_PRICE_ID
CRITICAL: sets client_reference_id = orgId
Returns session URL
createPortalSessionAction({ returnPath? })
Requires org tier === "pro" and stripe_customer_id
Returns portal URL
Stripe client is created lazily (no import-time env crash during next build).

Webhook — app/api/webhooks/stripe/route.ts
Created secure webhook:

Reads raw body with await request.text()
Verifies with stripe.webhooks.constructEvent
Handles:
checkout.session.completed: upgrades org → tier='pro', writes stripe_customer_id, stripe_subscription_id using service_role
customer.subscription.deleted: finds org by stripe_subscription_id, downgrades to tier='free' and clears subscription id
Also added /api/webhooks/stripe(.*) to public routes in middleware.ts.

6) UX interruption (modal) + header billing menu
Paywall modal — components/billing/paywall-modal.tsx
shadcn Dialog
Instrument Serif header text
Primary CTA: “Upgrade to Pro - $29/mo” (blue) + Framer Motion loading spinner
Calls createCheckoutSessionAction and redirects
Integration
components/uploads/project-dropzone.tsx: detects "PAYWALL_LIMIT_REACHED" and calls onPaywallLimitReached()
app/projects/[id]/project-detail-client.tsx: opens PaywallModal for upload gate and synthesize gate
Header
Created components/layout/header.tsx (server) + header.client.tsx (client dropdown):

Minimal “Billing” dropdown
If tier is pro, “Manage subscription” opens portal via createPortalSessionAction
Also added <Toaster /> to app/layout.tsx so your existing toasts (share-link + billing) actually render.

Verification
npm run lint ✅
npm run build ✅
npm run typecheck ✅
And I did not touch app/api/inngest/functions.ts per your directive.