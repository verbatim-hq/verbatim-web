### SYSTEM ROLE & DIRECTIVE
You are an elite, world-class full-stack engineer. You write hyper-optimized, strictly typed Next.js 15 App Router code. You do not write placeholders. You output complete, production-ready code. Apply a "Zero-Any" policy.

### CONTEXT: VERBATIM.AI — FEATURE 4: LLM SYNTHESIS ENGINE
We are building the "Brain" of the application. This pipeline takes the `transcript_json` generated in Feature 3 and uses Claude Sonnet 4.6 to extract structured themes and quotes. You are responsible for implementing our primary product moat: the **Citation Verification Loop**. Every single quote extracted by the LLM MUST be a verified exact substring of the original transcript.

### TECHNICAL CONSTRAINTS
1.  **AI Engine**: Claude Sonnet 4.6 via the `@anthropic-ai/sdk`.
2.  **Job Orchestration**: Inngest. This function triggers on the `source.transcribed` event.
3.  **Cost Observability**: Every LLM call must be logged to an `llm_calls` table with token counts and cost calculations.
4.  **Validation**: Strict Zod schemas for the LLM output (Tool Calling / Structured Outputs).
5.  **Security**: Background database writes MUST use the Supabase `service_role` key to bypass RLS, ensuring `org_id` is explicitly passed and stored.

### IMPLEMENTATION REQUIREMENTS

#### 1. SQL Migration (0005_reports_and_llm_calls.sql)
Initialize the tables for synthesis persistence and cost tracking:
- `reports`: id (uuid), org_id (text/FK), source_id (uuid/FK), status (text: 'synthesizing', 'completed', 'failed'), content_json (jsonb), created_at.
- `llm_calls`: id (uuid), org_id (text/FK), source_id (uuid/FK), model (text), prompt_tokens (int), completion_tokens (int), created_at.
- **Policies**: Apply `FOR ALL` operations `WHERE org_id = current_org_id()`. Keep default-deny posture.

#### 2. The Zod Schemas (`lib/synthesis.ts`)
Define and export the strictly typed output schema for Claude:
- `ThemeSchema`: `name` (string), `description` (string).
- `QuoteSchema`: `text` (string), `speaker` (string).
- `SynthesisReportSchema`: An array of themes, where each theme contains an array of quotes.

#### 3. The Inngest Function (`app/api/inngest/functions.ts`)
Create `verbatim/synthesis.generate` listening for the `source.transcribed` event. You must strictly utilize `step.run` for idempotency:
- **Step 1 (`step.run("initialize-report")`)**: Fetch the `transcript_json` and `org_id` from `sources` using `service_role`. Insert a new record into `reports` with status `synthesizing`.
- **Step 2 (`step.run("generate-and-verify-synthesis")`)**: 
    - Construct a system prompt explicitly commanding Claude to act as an exact-extraction engine.
    - Send the transcript text to Claude Sonnet 4.6 using tool calling to enforce `SynthesisReportSchema`.
    - **Observability**: Log the `usage` (prompt and completion tokens) directly into the `llm_calls` table immediately after the API returns.
    - **The Citation Verification Loop (CRITICAL MOAT)**:
        - Iterate through every quote returned by Claude.
        - Check if `quote.text` is an exact substring of the full transcript text. **Crucial:** You must normalize both strings (lowercase, `.replace(/\s+/g, ' ').trim()`) to prevent whitespace/newline mismatches from triggering false negatives.
        - *If ANY quote fails this check*: `throw new Error("Citation Verification Failed: Hallucinated quote detected.")`. (This deliberate exception will trigger Inngest's automatic retry mechanism for this specific step).
    - Return the validated payload.
- **Step 3 (`step.run("map-timestamps")`)**: For the verified quotes, map the `start` and `end` millisecond timestamps by cross-referencing the quote's words against the original `transcript_json` word array.
- **Step 4 (`step.run("finalize-report")`)**: Update the `reports` table with `status = 'completed'` and save the heavily validated, timestamped `content_json`.

#### 4. Engineering Standards
- **Strict TS**: Zero `any` types. Define complete interfaces for the Anthropic response and Inngest payload.
- **Idempotency**: By separating the database writes and the LLM generation into distinct `step.run` blocks, you prevent duplicate row creation during a retry loop.

### EXPECTED OUTPUT
1.  `npm install @anthropic-ai/sdk`
2.  The `0005_reports_and_llm_calls.sql` migration script.
3.  The strict Zod schemas inside `lib/synthesis.ts`.
4.  The complete Inngest function code (`app/api/inngest/functions.ts`) featuring the exact-substring verification loop and explicit step isolation.